<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.JustDoIt.Mecca.LCM.mapper.UserMapper">
    <insert id="insertUser">
        INSERT INTO USER_TBL VALUES (#{uMail}, #{uPassword}, #{uNickname}, '프로필 이미지', NULL, DEFAULT)
    </insert>
    <update id="updateUser">
        UPDATE USER_TBL
        SET U_NICKNAME = #{uNickname}, U_INTRODUCE = #{uIntroduce}
        WHERE U_MAIL = #{uMail}
    </update>
    <delete id="deleteUser">
        DELETE FROM USER_TBL WHERE U_MAIL = #{uMail}
    </delete>
    <select id="selectUser">
        SELECT * FROM USER_TBL WHERE U_MAIL = #{uMail} AND U_PASSWORD = #{uPassword}
    </select>
</mapper>